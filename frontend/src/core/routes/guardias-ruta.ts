/**
 * Guardias de Ruta (Navigation Guards)
 * Verifica si el usuario tiene sesi�n activa o permisos antes de entrar a rutas protegidas.
 */

// import router from './index';
// router.beforeEach((to, from, next) => { ... });
